using System;
using PierresBakery.Models;
using System.Collections.Generic;

namespace PierresBakery
{
    public class Program
    {
        public static void Main()
        {
            // int[] intArray = new int[5] { 10, 9, 13, 100, 1 };

            // foreach (int eleBeforeSorting in intArray)
            // {
            //     Console.WriteLine(eleBeforeSorting);
            // }

            // Console.WriteLine("Array after sorting is ...");
            // Array.Sort(intArray);

            // foreach (int eleAfterSorting in intArray)
            // {
            //     Console.WriteLine(eleAfterSorting);
            // }
            // // Console.WriteLine("Enter a sentence:");
            // // string source = Console.ReadLine();
            // // Console.WriteLine("Enter FROM string:");
            // // string from = Console.ReadLine();
            // // Console.WriteLine("Enter TO string:");
            // // string to = Console.ReadLine();

            // // FindR userSentence = new FindR();

            // string replacement = userSentence.ConvertSentence(source, from, to);

            // Console.WriteLine($"The source string is <{source}>");
            // Console.WriteLine($"The updated string is <{replacement}>");
        }
    }
}
